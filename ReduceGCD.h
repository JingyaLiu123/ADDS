#ifndef REDUCEGCD_H
#define REDUCEGCD_H
#include "ReduceGeneric.h"
#include <map>
#include <list>

class ReduceGCD: public ReduceGeneric
{
    public:
        int f(int);
        //std::vector<int> map(std::vector<int>);
};
#endif 