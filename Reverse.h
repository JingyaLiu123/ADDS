#ifndef REVERSE_H
#define REVERSE_H
#include <string>

class Reverse
{
    public:

    std::string reverseString(std::string letters);
    int reverseDigits(int value);
    std::string letters;
    int value;
};
#endif 